import PropTypes from "prop-types";
import React, { Component } from "react";

export default class PollContent extends Component {
  static propTypes = {};

  render() {
    return <div>PollContent</div>;
  }
}
